const generatePassword = (data) => {
  let twoWords = data[0],
    number = data[1],
    symbol = data[2],
    website = data[3],
    firtLetter = data[4],
    currenDate = new Date();
  let password = "";

  //Generate password code...

  return password;
};

// export default generatePassword;
